import pic from "../assets/Rectangle90.png";

const CardLogo = () => {
  return (
    <img
      src={pic}
      className="ppic"
      style={{ verticalAlign: "middle", marginBottom: "0" }}
    />
  );
};

export default CardLogo;
